<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Options for `xcodebuild -exportArchive`, used by `make ios-upload`.

  No teamID key: the Makefile resolves the team at build time and passes it,
  for the same reason it is absent from app/ios/project.yml -- the team
  identifier is not recorded in this repository.

  uploadSymbols is on so crash reports symbolicate. manageAppVersionAndBuildNumber
  is off: the build number should change because someone changed it in
  project.yml, not because Xcode silently bumped it mid-upload and left the
  repository disagreeing with App Store Connect about what shipped.
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store-connect</string>
    <key>destination</key>
    <string>export</string>
    <key>uploadSymbols</key>
    <true/>
    <key>manageAppVersionAndBuildNumber</key>
    <false/>
    <key>signingStyle</key>
    <string>automatic</string>
</dict>
</plist>
